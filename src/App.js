import './App.css';
import {DropDownComponent, TestComponent, SingIn, Button, InputField, CloseButton} from './components';

function App() {
  return (
      <div className="App">
        <DropDownComponent/>
        <TestComponent/>
        <SingIn/>
        <Button/>
        <InputField/>
        <CloseButton/>
      </div>
  );
}

export default App;
